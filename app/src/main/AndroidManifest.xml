<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.juegoproyectofinal">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JuegoProyectoFinal">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameOptionsActivity" />
        <activity android:name=".MemoryGameActivity" />
        <activity android:name=".SumGameActivity" />
        <activity android:name=".WordGameActivity" />
        <activity android:name=".MemoryScoresActivity" />
        <activity android:name=".SumScoresActivity" />
        <activity android:name=".WordScoresActivity" />
    </application>



</manifest>
